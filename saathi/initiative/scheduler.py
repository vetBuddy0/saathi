"""Decides *when* to speak first — scheduled, event, and noticed triggers.

Not built until checkpoint 3. Queries `identity/reflect.py`'s output for
things worth following up that haven't been (SPEC.md, "Initiative").
"""
