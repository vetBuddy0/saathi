"""Speaker output — not built at checkpoint 1.

Exists as the mirror of `capture.py`: turns audio from `voice/session.py`
into sound on the chosen output `Device`, and is also what feeds the
reference signal AEC needs in `aec.py`.
"""
