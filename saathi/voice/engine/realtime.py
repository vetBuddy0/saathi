"""Speech-to-speech `VoiceSession` — 500 ms voice-start budget.

Not built at checkpoint 1. The faster of the two engines in SPEC.md's
budget table; whether it exists at all depends on what checkpoint 2 finds
available, so this stays a stub until that conversation happens.
"""
