"""A LiveKit-backed `Transport` — later, not checkpoint 1 or 2.

Placeholder only: SPEC.md's module map lists it explicitly as the "(later)"
transport, after `websocket.py` and `local.py` exist and one of them turns
out not to be enough.
"""
